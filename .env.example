# Example environment variables for Vite (safe to commit)
# Copy to .env and fill in real values.
VITE_YOUTUBE_API_KEY=your_youtube_api_key_here

# Contact form endpoint (Cloudflare Worker route)
# Production example: https://blackbridgemindset.com/api/contact
# Local dev example: http://127.0.0.1:8787/api/contact
VITE_CONTACT_ENDPOINT=

# Schedule/booking API base.
# Recommended in production when the Worker is routed on the same domain: leave blank.
# Example when hosted elsewhere: https://blackbridgemindset.com
VITE_SCHEDULE_API_BASE=

# Newsletter subscribe endpoint.
# Default is /api/schedule/newsletter/subscribe
VITE_NEWSLETTER_SUBSCRIBE_ENDPOINT=

# Public link included in newsletters. Keep this on the same production domain.
NEWSLETTER_UNSUBSCRIBE_URL=https://blackbridgemindset.com/api/schedule/newsletter/unsubscribe
